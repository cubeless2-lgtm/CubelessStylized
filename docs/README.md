# CubelessStylized Docs Pointer

Durable CubelessStylized documents, learning notes, research, hooks documentation, review checklists, work logs, and study material now live in:

```text
../CubelessOps/docs/projects/cubeless-stylized/
```

This project `docs/` folder is intentionally thin. Keep only this pointer here unless a future tool truly needs a project-local file.

## Current Owner

Use the Ops index:

```text
../CubelessOps/docs/projects/cubeless-stylized/README.md
```

Project tools that still use legacy `docs/...` path strings should resolve them through:

```text
Tools/Unreal/cubeless_ops_paths.py
```
